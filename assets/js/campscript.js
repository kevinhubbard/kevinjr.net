window.addEventListener('DOMContentLoaded', (event) => {
	var qs = document.getElementsByName('checkbox');

	if (qs === 'checked' {
		console.log('selected');
	} else {
		console.log('unselected');
	}
});