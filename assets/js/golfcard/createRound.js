window.addEventListener("DOMContentLoaded", function(event) {
    event.preventDefault();

    const btn = document.getElementById('usrHostLink');
    btn.addEventListener('click', function(e){
        
    });
});